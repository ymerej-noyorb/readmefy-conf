FROM mysql:8.4
ENV MYSQL_ROOT_PASSWORD=${READMEFY_ROOT_PASSWORD}
ENV MYSQL_DATABASE=${READMEFY_DATABASE}
ENV MYSQL_USER=${READMEFY_USER}
ENV MYSQL_PASSWORD=${READMEFY_PASSWORD}
EXPOSE 3306
CMD ["mysqld"]